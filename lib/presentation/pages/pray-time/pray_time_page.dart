import 'package:flutter/material.dart';

class PrayTimesPage extends StatelessWidget {
  const PrayTimesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pray Times Page'),
    );
  }
}
